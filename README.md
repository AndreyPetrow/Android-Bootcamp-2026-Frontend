# Техническое задание на разработку программного продукта для планирования встреч

## Команда: нет минусов, одни си плюс плюсы

## 1. Общие описание
Необходимо разработать программный продукт для планирования встреч сотрудников внутри компании. Система состоит из клиентского мобильного приложения и серверной части.

## 2. Клиентская часть (Android-приложение)
Клиентом является **Android-приложение**, которое должно быть реализовано на **Kotlin** или **Java** с использованием **адаптивной верстки**.

Приложение должно предоставлять интуитивно понятный интерфейс, в котором реализован следующий функционал:

*   **Регистрация и авторизация** сотрудника.
*   **Настройка личного профиля**.
*   **Создание встречи** с возможностью приглашения других сотрудников. Приглашенные сотрудники должны иметь возможность дать бинарный ответ (**принял** / **не принял**).
*   **Просмотр списка активных приглашений** на встречи с возможностью дать ответ о возможности посещения.
*   **Просмотр собственного расписания встреч** в разрезе дня, недели и месяца.

**Важное условие:** временные слоты для встреч разбиты строго по часам. Например, можно создать встречу на 9:00–10:00, но создание встречи на 9:10, 9:05 и т.д. — невозможно.

## 3. Серверная часть
Сервер представляет собой **микросервис на Spring Boot**, работающий с СУБД **PostgreSQL** или **H2**.

Серверное приложение должно:
*   Предоставлять API, необходимый для полноценной работы мобильного приложения.
*   Осуществлять все основные операции с данными в базе через **Spring Data JPA**.
*   Использовать **Liquibase** для создания схемы базы данных и ее предзаполнения.


### **User Stories**

**Регистрация/Авторизация**
*   Как сотрудник, я хочу зарегистрироваться в приложении, чтобы получить доступ к системе планирования встреч.
*   Как сотрудник, я хочу войти в свой аккаунт, чтобы использовать приложение после регистрации или для использования приложения на дркгом устройстве.
*   Как сотрудник, я хочу, чтобы мои данные для входа сохранялись, чтобы не вводить их повторно при каждом запуске приложения.

**Управление профилем**
*   Как сотрудник, я хочу редактировать свой профиль (имя, должность, контакты и тп), чтобы коллеги могли меня идентифицировать.

**Управление встречами**
*   Как сотрудник, я хочу создавать встречи, выбирая часовой слот и приглашая других сотрудников, чтобы организовать совместное мероприятие.
*   Как сотрудник, я хочу видеть список активных приглашений на встречи, чтобы принять решение об участии.
*   Как сотрудник, я хочу иметь возможность ответить на приглашение (принять/отклонить), чтобы инициатор встречи знал о моем решении.
*   Как сотрудник, я хочу просматривать свое расписание встреч в разрезе дня, недели и месяца, чтобы эффективно планировать свое время.

### **Use Cases**

**Регистрация / Авторизация**
*   **RegisterUseCase**
    *   **Цель:** добавить новый аккаунт сотрудника в БД.
    *   **Акторы:** пользователь (сотрудник).
    *   **Предусловие:** аккаунт с такими данными не зарегистрирован.
    *   **Триггер:** пользователь нажимает кнопку "Зарегистрироваться".
    *   **Основной поток:** пользователь вводит данные (логин/email, пароль, имя), сервер подтверждает регистрацию, данные сохраняются для автоматического входа.
    *   **Альтернативный поток:** пользователь ввел некорректные или занятые данные -> сервер возвращает ошибку, она отображается на экране.
*   **LoginUseCase**
    *   **Цель:** получить доступ к приложению с существующими учетными данными.
    *   **Акторы:** пользователь (сотрудник).
    *   **Предусловие:** учетная запись существует.
    *   **Триггер:** пользователь нажимает кнопку "Войти".
    *   **Основной поток:** пользователь вводит логин/email и пароль, сервер подтверждает вход, данные сохраняются для следующего запуска, пользователь авторизован.
    *   **Альтернативный поток:** данные неверны -> сервер возвращает ошибку, она отображается на экране.
*   **AuthorizeUseCase**
    *   **Цель:** автоматически авторизовать пользователя при запуске приложения.
    *   **Акторы:** система.
    *   **Предусловие:** в локальном хранилище есть сохраненные валидные учетные данные.
    *   **Триггер:** вход в приложение
    *   **Основной поток:** система отправляет сохраненные данные (токен) на сервер, сервер подтверждает их валидность, пользователь авторизован.
    *   **Альтернативный поток:** данные невалидны (истек срок, пароль изменен) -> запускается экран входа (`LoginAccountUseCase`).

**Управление профилем**
*   **UpdateProfileUseCase**
    *   **Цель:** обновить информацию в профиле пользователя.
    *   **Акторы:** пользователь (сотрудник).
    *   **Предусловия:** пользователь авторизован.
    *   **Триггер:** пользователь нажимает "Редактировать профиль".
    *   **Основной поток:** пользователь вводит новые данные (имя, фамилия, должность, контакты), система отправляет их на сервер, сервер подтверждает обновление.
    *   **Альтернативный поток:** сервер вернул ошибку валидации -> система отображает сообщение об ошибке, данные не сохраняются.

**Управление встречами**
*   **CreateMeetingUseCase**
    *   **Цель:** создать новую встречу и разослать приглашения.
    *   **Акторы:** пользователь (инициатор встречи).
    *   **Предусловия:** пользователь авторизован.
    *   **Триггер:** пользователь нажимает "Создать встречу".
    *   **Основной поток:** пользователь выбирает дату, часовой слот (например, 14:00-15:00), вводит название, описание и выбирает сотрудников из списка. Система проверяет слот на пересечение с другими встречами у приглашенных. Сервер создает встречу, приглашения рассылаются.
    *   **Альтернативный сценарий:** выбранный слот недоступен у одного из приглашенных -> система предупреждает инициатора, встреча не создается.
*   **GetInvitationsUseCase**
    *   **Цель:** получить список активных (неотвеченных) приглашений для текущего пользователя.
    *   **Акторы:** пользователь (сотрудник).
    *   **Предусловия:** пользователь авторизован.
    *   **Триггер:** пользователь открывает раздел "Приглашения" или приходит push-уведомление.
    *   **Основной поток:** система отправляет запрос на сервер, получает и отображает список приглашений.
    *   **Альтернативный поток:** ошибка сети -> система отображает сообщение об ошибке.
*   **RespondToInvitationUseCase**
    *   **Цель:** отправить ответ на приглашение (принять/отклонить).
    *   **Акторы:** пользователь (приглашенный сотрудник).
    *   **Предусловия:** пользователь авторизован, у него есть активные приглашения.
    *   **Триггер:** пользователь нажимает кнопку "Принять" или "Отклонить" на конкретном приглашении.
    *   **Основной поток:** система отправляет ответ на сервер, сервер обновляет статус приглашения и расписание пользователя.
    *   **Альтернативный поток:** встреча была отменена инициатором во время ответа -> сервер возвращает ошибку, система уведомляет пользователя.
*   **ViewScheduleUseCase**
    *   **Цель:** отобразить расписание встреч пользователя за выбранный период.
    *   **Акторы:** пользователь (сотрудник).
    *   **Предусловия:** пользователь авторизован.
    *   **Триггер:** пользователь открывает раздел "Расписание" и выбирает период (день/неделя/месяц).
    *   **Основной поток:** система запрашивает у сервера список встреч за период, получает данные, форматирует и отображает их в виде календаря/списка.
    *   **Альтернативный поток:** за выбранный период нет встреч -> система отображает соответствующий заглушающий экран.

### **Frontend Tasks**

**Регистрация / Авторизация**
*   **AuthUseCase**
    *   **Data-слой:** Реализовать репозиторий `AuthRepository` с методом `authorize(token)`. Endpoint: `GET /api/auth/validate` или `POST /api/auth/refresh`.
    *   **Domain-слой:** Реализовать UseCase, который читает токен из `SecureSharedPrefs`, вызывает репозиторий и в зависимости от результата перенаправляет на главный экран или экран логина.
    *   **UI-слой:** Не требуется отдельного экрана (работает через Splash Screen).
*   **RegisterUseCase**
    *   **Data-слой:** В `AuthRepository` добавить метод `register(credentials, userData)`. Endpoint: `POST /api/auth/register`.
    *   **Domain-слой:** Реализовать UseCase, который валидирует данные, вызывает репозиторий и при успехе сохраняет токен и запускает авторизацию.
    *   **UI-слой:** Реализовать фрагмент регистрации с полями: email, пароль, подтверждение пароля, полное имя. Привязать к ViewModel.
*   **LoginUseCase**
    *   **Data-слой:** В `AuthRepository` добавить метод `login(credentials)`. Endpoint: `POST /api/auth/login`.
    *   **Domain-слой:** Реализовать UseCase, который вызывает репозиторий, при успехе сохраняет токен и завершает авторизацию.
    *   **UI-слой:** Реализовать фрагмент входа с полями: email, пароль и кнопками "Войти" / "Зарегистрироваться". Привязать к ViewModel.

**Управление профилем**
*   **UpdateAccountProfileUseCase**
    *   **Data-слой:** Реализовать репозиторий `UserRepository` с методом `updateProfile(userProfileDto)`. Endpoint: `PUT /api/users/{id}`.
    *   **Domain-слой:** Реализовать UseCase, который берет данные из UI, вызывает репозиторий и возвращает статус операции.
    *   **UI-слой:** Реализовать фрагмент редактирования профиля с формой и кнопкой сохранения. Связать с ViewModel.

**Управление встречами**
*   **CreateMeetingUseCase**
    *   **Data-слой:** Реализовать репозиторий `MeetingRepository` с методом `createMeeting(meetingRequest)`. Endpoint: `POST /api/meetings`. Добавить метод `getEmployees()` для выбора участников/поиск по email или имени. Endpoint: `GET /api/users`.
    *   **Domain-слой:** Реализовать UseCase, который собирает данные о встрече (время, участники), вызывает репозиторий и обрабатывает результат.
    *   **UI-слой:** Реализовать фрагмент создания встречи с полями: название, описание, выбор даты, выбор часа, выбор участников (список с поиском).
*   **GetInvitationsUseCase**
    *   **Data-слой:** В `MeetingRepository` добавить метод `getInvitations(status: PENDING)`. Endpoint: `GET /api/meetings/invitations?status=pending`.
    *   **Domain-слой:** Реализовать UseCase, который вызывает репозиторий и возвращает список приглашений.
    *   **UI-слой:** Реализовать фрагмент списка приглашений. Каждый элемент содержит данные о встрече и кнопки "Принять"/"Отклонить".
*   **RespondToInvitationUseCase**
    *   **Data-слой:** В `MeetingRepository` добавить метод `respondToInvitation(meetingId, response)`. Endpoint: `POST /api/meetings/{id}/respond`.
    *   **Domain-слой:** Реализовать UseCase, который принимает ID встречи и решение, вызывает репозиторий.
    *   **UI-слой:** Обработка нажатий на кнопки в элементе списка приглашений.
*   **ViewScheduleUseCase**
    *   **Data-слой:** В `MeetingRepository` добавить метод `getSchedule(startDate, endDate)`. Endpoint: `GET /api/meetings/schedule?from=&to=`.
    *   **Domain-слой:** Реализовать UseCase, который на основе выбранного периода (день/неделя/месяц) вычисляет даты `from` и `to`, вызывает репозиторий ИЛИ загрузка из SharedPreferences.
    *   **UI-слой:** Реализовать фрагмент расписания для переключения периодов и виджетом календаря для отображения встреч.
